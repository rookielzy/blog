function convertToRoman(num) {
  var roman = ['I', 'V', 'X', 'L', 'C', 'D', 'M'];
  var length = num.toString().length;
  var str = "";
  switch(length) {
    case 1:
      
  }
  function toRoman(testNum, tmp) {
    if (testNUm >= tmp) {
       str += 
    }
  }
}

convertToRoman(36);
